name: greentill
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 21.0.0+25

environment:
  sdk: ">=3.0.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  flutter_bloc: ^9.1.1
  shared_preferences: ^2.0.6
  image_picker: ^1.0.7
  http: ^1.6.0
  dio: ^5.9.1
  fl_country_code_picker: ^0.1.9+1
  carousel_slider: ^5.1.2
  modal_bottom_sheet:
  syncfusion_flutter_sliders: ^32.2.7
  url_launcher: ^6.1.6
  intl: ^0.20.2

  qr_code_scanner: ^1.0.1
  camera: ^0.10.5+9

  currency_picker: ^2.0.5
  flutter_pdfview: ^1.0.4
  path_provider: ^2.0.11
  cached_network_image: ^3.2.0
  modal_progress_hud_nsn: ^0.5.1
  share_plus: ^6.3.0
  # image_cropper removed (unused in current flow; avoids Flutter 3.24 incompatibilities)
  dotted_border: ^2.1.0
  crop_your_image: ^2.0.0

  firebase_messaging: ^16.1.1
  flutter_local_notifications: ^17.2.2
  firebase_crashlytics: ^5.0.7
  firebase_core: ^4.4.0
  datepicker_dropdown: ^1.0.1
  firebase_auth: ^6.1.4
  google_sign_in: ^6.1.6
  sign_in_with_apple: ^7.0.1
  the_apple_sign_in: ^1.1.1
  permission_handler: ^12.0.1
#  document_scanner: ^0.2.1
  coupon_uikit: ^0.2.0
  flutter_downloader: ^1.7.4
  smooth_page_indicator: ^1.1.0
  flutter_image_compress: ^2.4.0
  expandable: ^5.0.1
  path: ^1.8.0
  pull_to_refresh: ^2.0.0
  percent_indicator: ^4.0.1
  webview_flutter:
  flutter_html:
  flutter_app_badger: ^1.5.0
  flip_card: ^0.7.0
  flutter_svg: ^2.0.10
  flutter_slidable: ^3.1.0
  photo_manager: ^3.8.3
  in_app_purchase: ^3.2.0
  pdf: ^3.11.1
  archive: ^3.6.1

dependency_overrides:
  firebase_core_platform_interface: 6.0.2
  win32: 3.0.0
  flutter_lints: ^5.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.4

flutter_icons:
  image_path_android: "assets/images/Ic_app_logo.png"
  image_path_ios: "assets/images/Ic_app_logo_updated.png"
  android: true
  ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/2.0x/
    - assets/images/3.0x/
    - assets/branding/
    - assets/lang/en.json
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Avenir
      fonts:
        - asset: assets/fonts/Avenir-Black-03.ttf
          weight: 800
        - asset: assets/fonts/Avenir-Book-01.ttf
          weight: 400
        - asset: assets/fonts/Avenir-Heavy-05.ttf
          weight: 900
        - asset: assets/fonts/Avenir-Light-07.ttf
          weight: 300
        - asset: assets/fonts/Avenir-Medium-09.ttf
          weight: 500
        - asset: assets/fonts/Avenir-Roman-12.ttf
          weight: 400

  #    - family: AvenirBold
  #      fonts:
  #        - asset: assets/fonts/AvenirLTStd-Black.otf
  #
  #    - family: AvenirBook
  #      fonts:
  #        - asset: assets/fonts/AvenirLTStd-Book.otf
  #
  #    - family: AvenirRoman
  #      fonts:
  #        - asset: assets/fonts/AvenirLTStd-Roman.otf

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
